VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# EmailJS — Get free keys at https://www.emailjs.com (200 emails/month free)
# 1. Create account → Email Services → Add Gmail/Outlook
# 2. Email Templates → Create template for contact forms & invoices:
#    Variables supported: {{to_name}}, {{to_email}}, {{order_id}}, {{invoice_number}}, {{plan_name}}, {{amount}}, {{upi_ref}}, {{date}}, {{message}}
# 3. Account → Public Key
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
# Optional: Dedicated template for invoices (falls back to VITE_EMAILJS_TEMPLATE_ID if left blank)
VITE_EMAILJS_INVOICE_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# Google Gemini AI — Get FREE key at https://aistudio.google.com/app/apikey
# Primary AI Provider (15 req/min free)
VITE_GEMINI_API_KEY=

# Groq AI (Fallback) — Get FREE key at https://console.groq.com/keys
# Backup provider if Gemini rate limit is hit or unavailable (Llama 3.3 70B)
VITE_GROQ_API_KEY=

# Admin Portal Passcode (Default is axenova2026)
VITE_ADMIN_PASSCODE=
